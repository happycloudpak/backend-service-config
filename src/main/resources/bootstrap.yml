spring:
  application:
    name: backend-service-config
  profiles:
    active: local
    include: common
---
spring:
  application:
    name: global
  profiles:
    active: dev
    include: common,common-eureka
