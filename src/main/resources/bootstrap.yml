spring:
  cloud: 
    config:
      uri: ${config_servers:http://localhost:9001}
---
spring:
  application:
    name: global
  profiles:
    active: dev
    include: common,common-eureka
---    
spring:
  application:
    name: backend-service-config
  profiles:
    active: local
    include: common
    

